apiVersion: apps/v1
kind: Deployment
metadata:
  name: mesh-frontend
  labels:
    app: mesh
spec:
  replicas: 3
  selector:
    matchLabels:
      app: mesh
  template:
    metadata:
      labels:
        app: mesh
    spec:
      containers:
        - name: mesh
          image: mesh-frontend:latest
          ports:
            - containerPort: 80
